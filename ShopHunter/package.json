{
  "name": "shophunter",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "tsx src/server/index.ts",
    "db:migrate": "prisma migrate dev --name init && prisma generate",
    "db:generate": "prisma generate",
    "build": "yarn db:generate && tsc -p tsconfig.server.json && yarn build:client",
    "build:client": "cd client && yarn build",
    "start": "node dist/server/index.js",
    "postinstall": "yarn db:generate && tsc -p tsconfig.server.json && cd client && yarn install && yarn build"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@libsql/client": "^0.11.0",
    "@prisma/adapter-libsql": "^7.6.0",
    "@prisma/client": "^7.6.0",
    "cors": "^2.8.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.2",
    "prisma": "^7.6.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  },
  "prisma": {
    "schema": "prisma/schema.prisma"
  }
}
